name: kubernetes
defaults:
  NAMESPACE: default
deploy:
  - name: Build
    run: docker build -t $REGISTRY/$NAME:$VERSION .
  - name: Push
    run: docker push $REGISTRY/$NAME:$VERSION
  - name: Apply
    run: kubectl set image deployment/$NAME $NAME=$REGISTRY/$NAME:$VERSION -n $NAMESPACE
