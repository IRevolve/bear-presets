name: node
detection:
  files:
    - package.json
validation:
  setup:
    - name: Install
      run: npm ci
  lint:
    - name: Lint
      run: npm run lint --if-present
  test:
    - name: Test
      run: npm test --if-present
  build:
    - name: Build
      run: npm run build --if-present
